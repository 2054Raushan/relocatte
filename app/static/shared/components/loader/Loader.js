import React from 'react';
import './loader.css';

const Loader = () => {
    return (
        <div>Loading ...</div>
    );
}

Loader.propTypes = {
};

Loader.defaultProps = {
};

export default Loader;
